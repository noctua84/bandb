{{ template "base" . }}

{{ define "content" }}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ index .Data "title" }}</h1>
                <p>{{ index .Data "description" }}</p>
            </div>
        </div>
    </div>
{{ end }}

